<h1>BOOK ME! - homepage </h1>

<ul>
  <% @items.each do |item| %>
    <li>
      <%= item.title %>
      <%= item.author %>
      <%= item.description %>
    </li>
  <% end %>
</ul>

<button type="button" class="btn btn-primary">Dodaj</button>